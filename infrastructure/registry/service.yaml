---
apiVersion: v1
kind: Service
metadata:
  name: local-registry
  namespace: default
spec:
  type: NodePort
  selector:
    deployment: local-registry
  clusterIP: 10.101.67.151
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30005
